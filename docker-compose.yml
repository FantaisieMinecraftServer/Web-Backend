version: "3"
services:
  backend:
    container_name: backend
    build:
      context: ./app
    ports:
      - "8080:8080"
    volumes:
      - /app
    tty: true
    env_file:
      - ./app/.env
    environment:
      - TZ=Asia/Tokyo
