version: "3"
services:
  backend:
    container_name: backend
    build: .
    ports:
      - "6000:6000"
    working_dir: /app
    tty: true
    env_file:
      - ./app/.env
    environment:
      - TZ=Asia/Tokyo
